<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="phone">

	<insert id="insert" parameterType="Phone">
		INSERT INTO phone(num, model, price, vcode)
		VALUES(#{num}, #{model}, #{price}, #{vcode})
	</insert>
	
	<!-- Dynamic Query 사용 : selectAll + selectOne (num) 둘다 사용할 수 있게 -->
	<select id="select" resultType="phone" parameterType="Phone">
		SELECT num, model, price, vcode
		FROM phone 
		JOIN company USING(#{vcode})
		WHERE 
		<if test="num != null">
			num=#{num}
		</if>
	</select>

	
	<!-- Dynamic Query 사용 -->
	<delete id="delete" parameterType="String">
		DELETE FROM phone
		WHERE num=#{num}		
	</delete>
	
	<update id="update" parameterType="String">
		UPDATE phone 
		SET model=#{model}, price=#{price}, vcode=#{vcode}
		WHERE num=#{num}
	</update>
	
	<!-- Dynamic Query 사용 : id와 pw 둘 다 입력했을 때랑, id만 입력했을 때를 같이 처리할 수 있게 -->
	<select id="selectUser" resultType="phone" parameterType="UserInfo">
		SELECT num, model, price, vcode
		FROM phone
		WHERE 
		<if test="id !=null and pw != null">
			id = #{id} AND pw=#{pW}
		</if>
		<if test="id != null">
			AND id = #{id}
		</if>
		
	</select>
	
</mapper>